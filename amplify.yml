version: 1
applications:
  - appRoot: apps/frontend
    frontend:
      phases:
        preBuild:
          commands:
            - rm -rf node_modules
            - rm -rf apps/frontend/node_modules
            - corepack enable
            - pnpm install
        build:
          commands:
            - cd apps/frontend
            - pnpm build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'

  - appRoot: apps/sanity-studio
    frontend:
      phases:
        preBuild:
          commands:
            - rm -rf node_modules
            - rm -rf apps/sanity-studio/node_modules
            - corepack enable
            - pnpm install
        build:
          commands:
            - cd apps/sanity-studio
            - pnpm build
      artifacts:
        baseDirectory: ./dist
        files:
          - '**/*'